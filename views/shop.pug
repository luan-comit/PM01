extends ./_bootstrap
append styles
    head 
        link(rel="stylesheet", href="../css/style.css")
        link(href='../css/justified-nav.css', rel='stylesheet')
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='description', content='Some description')
        meta(name='viewport', content='width=device-width, initial-scale=1')
    include ./components/tables
    body
        // Jumbotron
        .container(class="col-8")
            .jumbotron
                h3 Your Shopping  
                    a.btn.btn-lg.btn-success(href='./cart')
                        span Cart 
                        span(id="cartCount", style="color: blue")  
                    a.mycart(href='./clearcart')
                        span Clear Cart
                .jumbotron
                    .table-responsive
                        table.table-striped
                            thead
                                tr
                                    th #
                                    th.thumnail-tbl Image
                                    th Click to buy
                                    th Description
                                    th Selling Price 
                                    th Original Price
                            tbody
                            each item in listItems
                                tr
                                    td= item.pos + 1
                                    td.thumbnail-tbl
                                            img.shop-img-td-tbl(src= item.img_src) 
                                    td
                                        a.addCart(href='#', style='white-space: nowrap') Add to Cart
                                    td= item.title
                                    td $#{item.price}
                                    td $#{item.price_list}
        .container
            footer.footer
                p © Shop & Payment - a JavaScript project of Luan Le
        script.
            const listItems = !{JSON.stringify(listItems)};
            const clearcart =  !{JSON.stringify(clearcart)};
            console.log(listItems);
            console.log("Clear cart: ", clearcart);

        script(src="../js/shop.js", defer)